using UnityEditor;

public static class PowerupVals
{
    public static bool shouldSpawnPowerups;

    // List all powerups
    public static bool
        speed,
        sizeIncrease,
        sizeDecrease;
}